# order-service
Construit en **Phase 1** du roadmap : REST endpoint POST /orders,
persistence Postgres (order_db), Transactional Outbox Pattern,
publication de OrderCreated sur le topic `order.events`.
